import announcement from "./announcement-schema";
import page from "./page-schema";
import publication from "./publication-schema";

const schemas = [publication, page, announcement];

export default schemas;